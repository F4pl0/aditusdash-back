export class ErrorCodes {

    // Client Error codes 4XX
    static BAD_REQUEST = 400;
    static UNAUTHORIZED = 401;
    static NOT_FOUND = 404;
    static PARAMETERS_NOT_STATISFIED = 452;

    // Server Error codes 5XX
    static GENERIC_SERVER_ERROR = 500;
}